# DevEnv Manager

## Концепция

Инструмент для управления локальными окружениями разработки через Telegram-бот.

**Язык реализации:** Python

## Проблема

При работе над несколькими проектами возникает необходимость:
- Переключаться между различными окружениями разработки
- Помнить, какие репозитории входят в каждое окружение
- Хранить конфигурации (.env, CLAUDE.md) для каждого окружения
- Удалённо взаимодействовать с Claude Code для работы над кодом

## Решение

### Реестр окружений

Централизованный каталог в формате YAML, описывающий все настроенные окружения:
- Название и описание окружения
- Путь к рабочей директории (cwd для Claude Code)

### Telegram-бот

Локально запускаемый бот для удалённого управления окружениями:
- Просмотр списка доступных окружений
- Активация/переключение между окружениями
- Запуск Claude Code в контексте выбранного окружения
- Получение ответов от Claude Code в Telegram
- Отправка команд и промптов через чат

## Ключевые компоненты

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   Telegram      │────▶│   DevEnv        │────▶│   Claude Code   │
│   Bot API       │◀────│   Manager       │◀────│   SDK           │
└─────────────────┘     └─────────────────┘     └─────────────────┘
                               │
                               ▼
                        ┌─────────────────┐
                        │   Реестр        │
                        │   окружений     │
                        │   (YAML)        │
                        └─────────────────┘
```

## Интеграция с Claude Code

Используется `claude-code-sdk` (Python 3.10+) для взаимодействия с Claude Code:
- Стриминг ответов — можно показывать прогресс в Telegram
- Метод `interrupt()` для прерывания выполнения
- `ResultMessage` сигнализирует о завершении задачи

## Команды бота (предварительно)

| Команда | Описание |
|---------|----------|
| `/start` | Приветствие, проверка доступа |
| `/list` | Список всех окружений |
| `/use <name>` | Выбрать активное окружение |
| `/status` | Текущее окружение и статус |
| `/ask <prompt>` | Отправить запрос в Claude Code |
| `/stop` | Прервать выполнение |

## Управление состоянием

**Проверено экспериментально:** SDK не блокирует параллельные запросы в одной директории — каждый вызов создаёт независимую сессию.

**Решение:** запрет параллельных запросов в одном окружении реализуется через Redis:
- Флаги занятости с TTL для защиты от зависших задач
- Клиенты SDK хранятся в памяти для возможности вызова `interrupt()`

## Конфигурация

### Секреты (.env)
- Токен Telegram-бота
- Список разрешённых user ID
- URL Redis
- Путь к файлу реестра окружений

### Реестр окружений (YAML)

```yaml
environments:
  trading-platform:
    description: "Торговая платформа"
    path: ~/projects/trading-platform

  blog:
    description: "Личный блог на Astro"
    path: ~/projects/blog
```

## Безопасность

- Бот отвечает только пользователям из списка разрешённых ID
- Секреты хранятся в .env (добавлен в .gitignore)

## Статус

🟡 В разработке — этап планирования
